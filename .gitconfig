[user]
	email = jalexvig@gmail.com
	name = Alex Vig
[core]
    editor = "if [ `uname` = Darwin ]; then ED='mvim -f'; else ED='vim'; fi; $ED"
[push]
	default = simple
[credential]
    helper = cache --timeout=14400
